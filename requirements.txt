# Streamlit Community Cloud installs this file by default.
# Keep it slim (no CUDA, no full TensorFlow) so the Cloud build succeeds.
# Full GPU training deps live in requirements-train.txt (local RTX 4060 work).
-r streamlit-requirements.txt
